apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: os-data1
  name: os-data1
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
